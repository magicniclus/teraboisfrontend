import React from 'react';

const Stars = (props) => {
    const stars = props.stars;

    return (
        <div className='starsContainer'>
            
        </div>
    );
};

export default Stars;